# Reflection

What was the most helpful aspect of this code’s structure?
The most helpful aspect is that this project taught us a bout the "object" a unique design where it can store strings and values. With this structure, we can add (using push) or modify the current objects.

What part was confusing or took longer to understand?
The part that was confusing and took longer to understand was the mostReadBook function. It uses an object to count how many times each book appears, then loops through that object to find the book with the highest count.